# دليل استخدام نظام إدارة المعلومات الطبية (IMS)

مرحباً بك في دليل الاستخدام السريع لنظام إدارة المعلومات الطبية (IMS). يوضح هذا الدليل كيفية تشغيل النظام وبيانات تسجيل الدخول للنقاط الطبية المختلفة.

---

## 🔑 كلمات المرور وبيانات الدخول

لتسجيل الدخول إلى النظام، يرجى اختيار النقطة الطبية وإدخال كلمة المرور المقابلة لها أدناه:

| النقطة الطبية | معرف النقطة (ID) | كلمة المرور (Password) |
| :--- | :--- | :--- |
| **نقطة النصيرات الطبية (أبو مدين)** | `nuseirat` | **`nu2026`** |
| **نقطة المجايدة** | `mujayda` | **`m2026`** |
| **نقطة دير البلح** | `deirbalah` | **`d2026`** |

---

## 🚀 كيفية تشغيل واستخدام النظام

1. **التشغيل بنقرة واحدة:**
   * اذهب إلى مجلد المشروع وانقر نقرًا مزدوجًا على ملف **`start_system.bat`**.
   * سيقوم الملف تلقائياً بالتحقق من بيئة العمل وتشغيل السيرفر في الخلفية، ثم فتح واجهة النظام في متصفح جوجل كروم أو إيدج تلقائياً.

2. **الوصول اليدوي:**
   * بعد تشغيل السيرفر، يمكنك الوصول للنظام في أي وقت بفتح الرابط التالي في المتصفح:
     [http://localhost:3001/IMS_Medical_System_4.html](http://localhost:3001/IMS_Medical_System_4.html)

3. **إيقاف النظام:**
   * لإيقاف السيرفر، يمكنك ببساطة إغلاق نافذة موجه الأوامر (Command Prompt) السوداء التي تعمل في الخلفية.
